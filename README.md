If a silo receives the explosion virtual signal when a rocket is ready, it will launch the pod towards its own planet.
The rocket will be launched even if there is no cargo, so consider only sending the signal once loading is complete.

Currently only supports Nauvis, since it was created to work with the Everything on Nauvis mod.